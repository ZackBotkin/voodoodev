<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
    </head>
    <body>
        <% if (user) { %>
            <h2> Hello <%=user%>
            <a href= "/logout">Logout</a>
        <% } else { %>
            <a href= "/register">Register</a>
            <a href= "/login">Login</a>
        <% } %>
    </body>
</html>
